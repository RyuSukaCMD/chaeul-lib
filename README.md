# chaeul-lib

Command & library untuk **Chaeul Bot**. Folder `command/` & `lib/` di sini
di-sync otomatis oleh bot saat start (auto-update).

- `command/` — seluruh command bot (per kategori)
- `lib/` — helper/library bot

Jangan taruh `sync.js` di sini (itu milik repo utama).
